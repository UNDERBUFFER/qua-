
class Main {
    static get(request, response) {
        response.render('main/main.hbs');
    }
    static post(request, response) {
        response.render('main/main.hbs');
    }
}

module.exports = Main
